#include <stdio.h>
void main () {

    printf ("Inclua tres valores: ");
    int A, B, C; 
    scanf ("%i %i %i", &A, &B, &C);
    printf ("Ta aqui: %i, %i, %i \n", A, B, C);

if (A>=B && A>=C) {
print
}