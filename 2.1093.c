/* Desafio 10 da Avaliação 2.
Desafio 1093 do URI Online Judge 
Manuella Porto e Tiago Freitas
Início de Abril de 2020*/ 

/*A entrada consiste de vários casos de teste. Cada caso de teste consiste de uma única linha,
contendo 4 inteiros EV1, EV2, AT e D separados por espaços (1 ≤ EV1, EV2 ≤ 10, 1 ≤ AT ≤ 5 and
1 ≤ D ≤ 10). O final da entrada é indicado por uma linha contendo quatro zeros, separados por
espaços */

/*1 ≤ D ≤ 10). O final da entrada é indicado por uma linha contendo quatro zeros, separados por
espaços*/

#include <stdio.h>
int main() {
    int av1, av2, at, d;
    
    do{
        scanf ("%d %d %d %d", &av1, &av2, &at, &d);
        if (av1!=0 && av2!=0 && at!=0 && d!=0 ){






            
        }
    }