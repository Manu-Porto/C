#include <stdio.h>
int main (){
    float A, B, C; 
    scanf ("%f %f %f", &A, &B, &C);

    float T, ;
    T = (A * B) / 2 ;
    C = C*C * 3.14159

    printf("NUMBER = %i \n", N); 
    printf("SALARY = U$ %.2f", R); 
return 0;
}